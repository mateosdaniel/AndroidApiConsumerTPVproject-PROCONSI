<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
        We control dark/light manually via ThemeManager (user preference),
        NOT via the system night mode. This file intentionally left minimal
        so the system night-mode qualifier does NOT override our manual theme.
    -->
</resources>